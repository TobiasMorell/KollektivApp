server {
    listen 80 ;
    server_name *;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name *;

    location / {
        include /config/nginx/proxy.conf;
        resolver 127.0.0.11 valid=30s;
        set $upstream preact-frontend;
        proxy_pass http://$upstream:80;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }
}
